version: '3.8'
services:
  api:
    build: ./docker/api
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
  dashboard:
    build: ./docker/dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./src:/app/src
